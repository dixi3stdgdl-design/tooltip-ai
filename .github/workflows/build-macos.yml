name: Build TooltipAI for macOS

on:
  push:
    branches: [main]
    paths:
      - 'TooltipAI.Core/**'
      - 'TooltipAI.Platform.Mac/**'
      - '.github/workflows/build-macos.yml'
  workflow_dispatch:

env:
  DOTNET_VERSION: '8.0.x'
  APP_NAME: 'Tooltip AI'
  BUNDLE_ID: 'com.mimo.tooltipai'
  VERSION: '1.0.0'

jobs:
  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        runtime: [osx-arm64, osx-x64]

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install macOS Workload
        run: dotnet workload install macos

      - name: Restore
        run: dotnet restore TooltipAI.Platform.Mac/TooltipAI.Platform.Mac.csproj

      - name: Build Core
        run: dotnet build TooltipAI.Core/TooltipAI.Core.csproj -c Release --no-restore

      - name: Publish
        run: |
          dotnet publish TooltipAI.Platform.Mac/TooltipAI.Platform.Mac.csproj \
            -c Release \
            -r ${{ matrix.runtime }} \
            --self-contained \
            -p:PublishTrimmed=true \
            -p:LinkMode=None

      - name: Create App Bundle
        run: |
          RUNTIME=${{ matrix.runtime }}
          APP_BUNDLE="./publish-${RUNTIME}/TooltipAI.app"
          mkdir -p "${APP_BUNDLE}/Contents/MacOS"
          mkdir -p "${APP_BUNDLE}/Contents/Resources"

          # Find the published binary
          PUBLISH_DIR="TooltipAI.Platform.Mac/bin/Release/net8.0-macos/${RUNTIME}/publish"
          if [ ! -d "$PUBLISH_DIR" ]; then
            PUBLISH_DIR="TooltipAI.Platform.Mac/bin/Release/net8.0-macos/${RUNTIME}"
          fi

          # Find and copy the main executable
          EXECUTABLE=$(find "$PUBLISH_DIR" -maxdepth 1 -type f -name "TooltipAI*" ! -name "*.dll" ! -name "*.pdb" | head -1)
          if [ -z "$EXECUTABLE" ]; then
            EXECUTABLE=$(find "$PUBLISH_DIR" -maxdepth 1 -type f -executable | head -1)
          fi

          if [ -z "$EXECUTABLE" ]; then
            echo "Available files in $PUBLISH_DIR:"
            ls -la "$PUBLISH_DIR/"
            echo "ERROR: Could not find executable"
            exit 1
          fi

          cp "$EXECUTABLE" "${APP_BUNDLE}/Contents/MacOS/TooltipAI"
          chmod +x "${APP_BUNDLE}/Contents/MacOS/TooltipAI"

          cat > "${APP_BUNDLE}/Contents/Info.plist" << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key><string>TooltipAI</string>
              <key>CFBundleIdentifier</key><string>com.mimo.tooltipai</string>
              <key>CFBundleName</key><string>TooltipAI</string>
              <key>CFBundleDisplayName</key><string>Tooltip AI</string>
              <key>CFBundleVersion</key><string>1.0.0</string>
              <key>CFBundleShortVersionString</key><string>1.0.0</string>
              <key>CFBundlePackageType</key><string>APPL</string>
              <key>LSMinimumSystemVersion</key><string>12.0</string>
              <key>NSHighResolutionCapable</key><true/>
          </dict>
          </plist>
          PLIST

      - name: Create DMG
        run: |
          hdiutil create -volname "TooltipAI" \
            -srcfolder "./publish-${{ matrix.runtime }}/TooltipAI.app" \
            -ov -format UDZO \
            "./publish-${{ matrix.runtime }}/TooltipAI-mac-${{ matrix.runtime }}.dmg"

      - name: Generate Hashes
        run: |
          cd ./publish-${{ matrix.runtime }}
          shasum -a 256 TooltipAI-mac-${{ matrix.runtime }}.dmg > TooltipAI-mac-${{ matrix.runtime }}.dmg.sha256

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: tooltipai-mac-${{ matrix.runtime }}
          path: ./publish-${{ matrix.runtime }}/TooltipAI-mac-${{ matrix.runtime }}.dmg

      - name: Upload App Bundle
        uses: actions/upload-artifact@v4
        with:
          name: tooltipai-mac-${{ matrix.runtime }}-app
          path: ./publish-${{ matrix.runtime }}/TooltipAI.app

  release:
    runs-on: ubuntu-latest
    needs: build-macos
    if: success()
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download ARM64 DMG
        uses: actions/download-artifact@v4
        with:
          name: tooltipai-mac-osx-arm64

      - name: Download x64 DMG
        uses: actions/download-artifact@v4
        with:
          name: tooltipai-mac-osx-x64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}-mac
          name: "TooltipAI v${{ env.VERSION }} (macOS)"
          body: |
            ## macOS Release

            ### Downloads
            - **Apple Silicon (M1/M2/M3):** `TooltipAI-mac-osx-arm64.dmg`
            - **Intel:** `TooltipAI-mac-osx-x64.dmg`

            ### Installation
            1. Download the DMG for your architecture
            2. Open the DMG and drag "Tooltip AI" to Applications
            3. On first launch, grant Accessibility permissions:
               System Preferences > Privacy & Security > Accessibility > Tooltip AI
            4. Grant Screen Recording permissions:
               System Preferences > Privacy & Security > Screen Recording > Tooltip AI

            ### Permissions Required
            - **Accessibility:** To read UI element information
            - **Screen Recording:** To detect mouse position via CGEventTap

            ### SHA-256 Hashes
            See attached .sha256 files for integrity verification.
          draft: false
          prerelease: false
          files: |
            tooltipai-mac-osx-arm64.dmg
            tooltipai-mac-osx-x64.dmg
